# API Base URL (default: http://localhost:8787)
VITE_API_BASE=http://localhost:8787

# Use fixtures instead of real API (1 = enabled, 0 = disabled)
# When enabled, all GET requests will load from /fixtures/*.json
# and all write requests will return mock success responses
VITE_USE_FIXTURES=1
